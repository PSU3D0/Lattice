[package]
name = "llm-provider-openai"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "MIT"
repository.workspace = true
homepage.workspace = true
description = "OpenAI provider for Lattice LLM. Derived from rig (MIT, Playgrounds Analytics Inc.)."

[lib]
name = "llm_provider_openai"

[dependencies]
llm-types = { path = "../llm-types" }
llm-agent = { path = "../llm-agent" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
schemars = "1"
tracing.workspace = true
tracing-futures = "0.2"
base64 = "0.22"
bytes = "1"
futures = "0.3"
http = "1"
async-stream = "0.3"

[dev-dependencies]
