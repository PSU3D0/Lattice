[package]
name = "kernel-plan"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Flow IR validation and lowering into executable plans."

[dependencies]
dag-core = { path = "../dag-core" }
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
uuid.workspace = true

[dev-dependencies]
proptest.workspace = true
dag-macros = { path = "../dag-macros" }
capabilities = { path = "../capabilities" }
kernel-exec = { path = "../kernel-exec" }
# Golden-fixture inputs for the FlowRequirements manifest (packet C1).
# Dev-dependency cycles through host-inproc are permitted by cargo.
example-s1-echo = { path = "../../examples/s1_echo" }
example-s12-sheetport-quote = { path = "../../examples/s12_sheetport_quote" }
