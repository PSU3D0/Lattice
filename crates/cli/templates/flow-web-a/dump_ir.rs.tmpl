//! Print this flow's Flow IR as JSON so it can be piped into
//! `flows-cli graph check`. Generated by `flows new`; native-only helper.

fn main() {
    let ir = example_{{NAME_SNAKE}}::flow();
    println!(
        "{}",
        serde_json::to_string(&ir).expect("serialize flow IR")
    );
}
