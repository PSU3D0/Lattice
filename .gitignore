target/
.target/
.worktrees/

# Local planning / issue trackers (never commit)
.hive/
docs/plans/

# Decrypted private docs (never commit plaintext)
private/
ref-libs/

# Local secret-manager config (copy from fnox.example.toml when needed)
fnox.toml
